% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coef_table_df.R
\name{coef_table_df}
\alias{coef_table_df}
\title{Gerar tabela de coeficientes de um ajuste ARIMA em formato \code{data.frame}}
\usage{
coef_table_df(fit, compact = FALSE)
}
\arguments{
\item{fit}{Objeto de modelo ARIMA aceito por \code{summary_arima_custom()}.}

\item{compact}{Se \code{TRUE}, utiliza \code{summary_arima_compact()} para
remover coeficientes com estimativa zero (padrão: \code{FALSE}).}
}
\value{
Um \code{data.frame} com a coluna \code{coef} na primeira posição e
as demais colunas geradas pelo resumo selecionado.
}
\description{
Constrói uma tabela de coeficientes a partir de um modelo ARIMA, usando
\code{summary_arima_custom()} (padrão) ou \code{summary_arima_compact()}
(modo compacto). Em seguida, garante a presença da coluna \code{coef} como
primeira coluna e remove nomes de linha.
}
\examples{
\dontrun{
  fit <- forecast::Arima(AirPassengers, order = c(1,1,1))
  coef_table_df(fit)
  coef_table_df(fit, compact = TRUE)
}

}
\seealso{
\code{\link{summary_arima_custom}}, \code{\link{summary_arima_compact}}
}
