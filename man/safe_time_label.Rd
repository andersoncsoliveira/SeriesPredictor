% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe_time_label.R
\name{safe_time_label}
\alias{safe_time_label}
\title{Obter rótulo temporal de forma segura em um objeto \code{ts}}
\usage{
safe_time_label(x_ts, t_index)
}
\arguments{
\item{x_ts}{Objeto de série temporal (tipicamente \code{ts} ou similar),
do qual será extraído \code{time(x_ts)}.}

\item{t_index}{Índice (1-based) da posição desejada no vetor de tempos.}
}
\value{
Uma string com o rótulo de tempo correspondente, ou
\code{NA_character_} em caso de falha/índice inválido.
}
\description{
Retorna o rótulo de tempo (via \code{time()}) associado a um índice
específico de uma série temporal. Caso \code{time(x_ts)} não esteja
disponível, ou o índice seja inválido, retorna \code{NA_character_}.
}
\examples{
x <- ts(1:12, start = c(2020, 1), frequency = 12)
safe_time_label(x, 1)
safe_time_label(x, 12)
safe_time_label(x, 999)  # NA

}
